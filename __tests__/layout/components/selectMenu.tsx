import React from 'react'
import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom"

describe("<SelectMenu", () => {
  //beforeEach(() => {})
  it("select-menu is in the document", () => {})
  it("All expected menu items are in the document", () => {})
  it("default value is selected on load", () => {})
  it("clicking on select menu renders menu-list", () => {})
  it("clicking on a list item calls handleSelect with val as arg", () => {})
})