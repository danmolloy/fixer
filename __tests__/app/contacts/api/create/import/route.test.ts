import { createContacts } from "../../../../../../app/contacts/api/create/import/route";

describe("createContacts()", () => {
  it("successfully creates all contacts", () => {})
  it("returns empty array and doesn't call prisma if no contacts are provided", () => {})
  it("handles errors in contact creation", () => {})
  it("handles DB error", () => {})
})