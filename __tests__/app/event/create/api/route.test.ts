import { formattedCalls, eventObj, createEvent } from "../../../../../app/event/create/api/route";

describe("formattedCalls()", () => {
  it("returns expected array", () => {})
})
describe("eventObj", () => {
  it("returns expected obj", () => {})
  it("calls formattedCalls", () => {})
})
describe("createEvent", () => {
  it("prisma.event.create resolves", () => {})
})