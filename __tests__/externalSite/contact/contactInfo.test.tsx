import { act, fireEvent, render, screen } from "@testing-library/react";
import "@testing-library/jest-dom"

describe("<ContactInfo />", () => {
  //beforeEach(() => {})
  it("contact-info is in the document", () => {})
  it("Header text is in the document", () => {})
  it("Helper text is in the document", () => {})
  it("Contact number is in the document", () => {})
  it("Contact email is in the document", () => {})
})