import { act, fireEvent, render, screen } from "@testing-library/react";
import "@testing-library/jest-dom"

describe("<ContactForm />", () => {
  //beforeEach(() => {})
  it("contact-form is in the document", () => {})
  it("First Name input is in the document with label", () => {})
  it("Last Name input is in the document with label", () => {})
  it("Contact number input is in the document with label", () => {})
  it("Contact email input is in the document with label", () => {})
  it("Message input is in the document with label", () => {})
  it("Submit button is in the document with label", () => {})
  it("If session, contact info is autofilled", () => {})
})